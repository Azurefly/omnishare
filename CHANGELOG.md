# Changelog

## 1.3.0-rc1

### Security

- 默认仅监听回环地址，局域网模式强制配置强访问密钥。
- 删除查询参数密钥和前端 URL 密钥传播；浏览器仅会话级保存密钥。
- 密钥改为 PBKDF2-HMAC-SHA256 派生存储，并支持旧版明文配置一次性迁移。
- 增加 CSP、严格 CORS/Host 校验、no-store、no-referrer、鉴权退避与安全审计。
- 增加短时文件媒体票据；HTML、SVG、XML 等主动内容禁止同源内联。
- Service Worker 仅缓存版本化静态资源。
- 局域网发现升级为 Ed25519 签名协议和稳定节点身份。

### Reliability

- 所有状态修改使用候选副本，持久化成功后才发布到内存。
- 增加文件/目录同步、上一代备份恢复、状态 schema 和跨平台单实例锁。
- 修复底层 Blob 丢失后的去重错误；逻辑文件与物理 Blob 分离。
- 备份增加一致性快照、配置、文件清单、SHA-256 校验和恢复回滚。
- 永久删除增加物理文件暂存和失败审计。

### Product and UX

- 修复过期、HEAD、Raw、阅后即焚、最大读取次数和分享计数语义。
- 敏感笔记列表默认脱敏，主动读取才消耗次数。
- 文档自动保存改为按文档快照串行执行，冲突保留本地草稿。
- 多文件上传限制为最多三个并发任务。
- Linux/Windows 安装卸载只管理本安装实例并保留用户数据。

### Engineering

- 合并前加固：文档列表不再返回正文，配置 API 隐藏本机数据目录，初始化失败可见化，Actions 依赖锁定完整提交 SHA。
- 版本统一由根目录 `VERSION` 管理并在构建前校验。
- 五轮测试加入 Race Detector、安全对抗、故障恢复、六目标格式检查和真实 Linux 安装卸载。
- 版本降级为 RC；未解决的单 JSON 扩展性、角色权限、自动 TLS、平台签名等不再宣称完成。

## 1.2.0

- 增加回收站、对象级分享、文件去重与重命名。
- 补齐随手记详情 API 和五轮验收框架。

## 1.1.0

- 建立 Go 单二进制、静态前端、JSON 存储基线。
- 完成随手记、文件视频、协同文档、设备发现、备份和六平台交付雏形。
