# Security Policy

## 支持版本

| 版本 | 状态 |
|---|---|
| 1.3.0-rc1 | 当前 RC，接受安全修复 |
| 1.2.x 及更早 | 不再支持；存在已公开的发布阻断风险 |

## 报告安全问题

不要在公开 Issue 中披露访问绕过、令牌泄露、路径遍历、任意文件读写、远程执行或数据破坏问题。请使用 GitHub 仓库的私密安全报告能力或仓库所有者公布的私密联系方式，并提供受影响版本、复现步骤、实际影响、日志/证据和建议修复。

## 安全部署底线

- 默认保持 `127.0.0.1` 监听；只有在设置至少 16 字节访问密钥后才启用局域网。
- 不要把 HTTP 服务直接暴露到公网。跨主机使用 HTTPS、Tailscale 或可信 VPN。
- 不要通过 URL、聊天截图或代理日志传播管理密钥。
- 对外分享设置有效期和访问次数；敏感内容使用独立渠道确认接收者。
- 数据目录权限应仅属于当前用户，并定期执行可恢复性验证，而不只是生成 ZIP。
- 未签名 RC 构建不应用于涉密、高合规或不可丢失数据场景。

## 已知未解决边界

- 单管理员密钥，没有角色、作用域令牌和设备级撤销。
- 分享链接仍是持有者能力令牌，没有分享密码和接收者身份认证。
- TLS 需要用户配置证书，没有自动信任引导。
- 单 JSON 状态不适合高并发或超大规模数据。
- Windows/macOS 发布物尚未签名、公证。
